diff -rupN a/tools/objtool/Makefile b/tools/objtool/Makefile
--- a/tools/objtool/Makefile	2026-02-23 08:55:32.000000000 +0100
+++ b/tools/objtool/Makefile	2026-02-23 13:56:37.949389158 +0100
@@ -91,8 +91,8 @@ LIBOPCODES_LIBS := $(shell \
 		    "-lopcodes -lbfd -liberty -lz"; do \
 		echo 'extern void disassemble_init_for_target(void *);' \
 		     'int main(void) { disassemble_init_for_target(0); return 0; }' | \
-			$(HOSTCC) -xc - -o /dev/null $$libs 2>/dev/null && \
-			echo "$$libs" && break; \
+			$(HOSTCC) -xc - -o /dev/null $libs 2>/dev/null && \
+			echo "$libs" && break; \
 	done)
 
 # Styled disassembler support requires binutils >= 2.39
