diff -rupN a/socket.c b/socket.c
--- a/socket.c	2026-02-10 11:32:49.000000000 +0100
+++ b/socket.c	2026-03-31 00:46:37.983203634 +0200
@@ -136,8 +136,9 @@ static int send6(struct wg_device *wg, s
 			if (cache)
 				dst_cache_reset(cache);
 		}
-		dst = ipv6_stub->ipv6_dst_lookup_flow(sock_net(sock), sock, &fl,
-						      NULL);
+
+		dst = ip6_dst_lookup_flow(sock_net(sock), sock, &fl, NULL);
+
 		if (IS_ERR(dst)) {
 			ret = PTR_ERR(dst);
 			net_dbg_ratelimited("%s: No route to %pISpfsc, error %d\n",
